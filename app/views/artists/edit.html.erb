<h1>My edit</h1>

<%= form_for @artist do |f| %>
    <label for="artist_name">My artist name:</label>
    <%= f.text_field :name %>
    <label for="artist_bio">My artist bio:</label>
    <%= f.text_area :bio %>
    <%= f.submit %>
<% end %>